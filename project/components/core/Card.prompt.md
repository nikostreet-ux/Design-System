Contenedor genérico de contenido.

```jsx
<Card variant="flat" hoverable>…</Card>
<Card variant="subtle" padding="var(--space-6)">…</Card>
```

`flat` por defecto (borde #EEEEEE). `elevated` para paneles flotantes, `subtle` para bloques informativos sobre blanco, `inverse` para destacados sobre slate.
