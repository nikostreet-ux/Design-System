Chip redondeado para filtros de catálogo.

```jsx
<Tag selected onClick={…}>2 plazas</Tag>
<Tag removable onRemove={…}>Rojo</Tag>
```

Es el único componente con radio pill del sistema; todo lo demás usa 2–8px.
